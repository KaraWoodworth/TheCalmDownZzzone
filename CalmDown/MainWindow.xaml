<Window x:Class="CalmDown.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CalmDown"
        mc:Ignorable="d"
        Title="MainWindow" Height="550
        " Width="800">
    <Window.Resources>
        <Style x:Key="GridStyle1" TargetType="{x:Type Grid}"/>
    </Window.Resources>

    <Grid Style="{DynamicResource GridStyle1}" Background="#FF2B3964" Margin="0,0,0,-6" RenderTransformOrigin="0.5,0.5">
        <Grid.RenderTransform>
            <TransformGroup>
                <ScaleTransform/>
                <SkewTransform/>
                <RotateTransform Angle="-0.08"/>
                <TranslateTransform/>
            </TransformGroup>
        </Grid.RenderTransform>
        
        <Rectangle Fill="#FF4050A0" RadiusX="20" RadiusY="20" Margin="24,23,24,23" />
        <Frame x:Name="AppFrame" />
    </Grid>

</Window>
